# DEPRECATED — React/TypeScript Frontend

This directory contains the original React + TypeScript frontend for the Tauri-based version of Murmur.

**Superseded by:** `src-swift/` (pure Swift + AppKit)

Safe to delete once the Swift version is confirmed stable in production.
