# DEPRECATED — Rust/Tauri Backend

This directory contains the original Rust + Tauri backend for the browser-based version of Murmur.

**Superseded by:** `src-swift/` (pure Swift + AppKit)

Safe to delete once the Swift version is confirmed stable in production.
