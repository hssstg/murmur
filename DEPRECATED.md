# DEPRECATED — Tauri/Vite Build Files

The following files belong to the original Tauri + React + TypeScript stack and are no longer used:

- `index.html`
- `package.json`
- `pnpm-lock.yaml`
- `vite.config.ts`
- `tsconfig.json`
- `tsconfig.node.json`
- `node_modules/`

**Superseded by:** `src-swift/` (pure Swift + AppKit, built with `swift build`)

Safe to delete once the Swift version is confirmed stable in production.
